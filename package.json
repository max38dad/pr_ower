{
  "name": "proxy-gateway",
  "version": "1.0.0",
  "type": "module",
  "description": "High-performance reverse proxy gateway for distributed scraping at edge",
  "scripts": {
    "start": "node --max-old-space-size=512 --expose-gc src/index.js",
    "dev": "node --watch --max-old-space-size=512 src/index.js"
  },
  "dependencies": {
    "fastify": "^5.3.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
